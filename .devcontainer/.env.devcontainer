# Devcontainer environment overrides
# These replace localhost references with Docker service names
# since the devcontainer runs on the same sentinel-net network.

# Database — connect via Docker service name instead of localhost:3510
CONTROL_DATABASE_URL=postgres://sentinel:sentinel@postgres:5432/sentinel_control

# Redis — connect via Docker service name instead of localhost:3511
REDIS_URL=redis://redis:6379

# Observability — Docker service names instead of localhost
LOKI_URL=http://loki:3100
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318

# Frontend API URL — still localhost since browser runs on your machine
NEXT_PUBLIC_API_URL=http://localhost:3500
