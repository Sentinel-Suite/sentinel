export const metadata = {
  title: "Sentinel Suite Documentation",
  description: "Sentinel Suite - Documentation",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
